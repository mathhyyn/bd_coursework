function open_workout_info(id) {
    document.getElementById("block_" + id).style.display = "flex";
}

function close_workout_info(id) {
    document.getElementById("block_" + id).style.display = "none";
}